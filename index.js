/**
 * @format
 */

import { AppRegistry } from 'react-native';
import App from './App';

// DŮLEŽITÉ: Registrujeme to jako 'main', protože to iOS vyžaduje
AppRegistry.registerComponent('main', () => App);
